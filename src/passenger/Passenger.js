import React from 'react';
import PassengerList from './PassengerList';

const Passenger = () => {
    return (
        <div>
            <PassengerList/>
        </div>
    );
};

export default Passenger;